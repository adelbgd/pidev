<form method="post" action="{{ path('app_ligne_panier_delete', {'idLigne': ligne_panier.idLigne}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_panier.idLigne) }}">
    <button class="btn">Delete</button>
</form>
