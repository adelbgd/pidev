<form method="post" action="{{ path('app_relation_follow_delete', {'id': relation_follow.id}) }}" onsubmit="return confirm('Are you sure you want to unfollow?');">

    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ relation_follow.id) }}">
    <button class="btn">Delete</button>

</form>
