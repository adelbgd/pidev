{% extends 'base.html.twig' %}

{% block title %}LignePanier index{% endblock %}

{% block body %}
    <h1>LignePanier index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdLigne</th>
                <th>NomProduit</th>
                <th>Description</th>
                <th>Image</th>
                <th>IdPanier</th>
                <th>IdProduit</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ligne_panier in ligne_paniers %}
            <tr>
                <td>{{ ligne_panier.idLigne }}</td>
                <td>{{ ligne_panier.nomProduit }}</td>
                <td>{{ ligne_panier.description }}</td>
                <td>{{ ligne_panier.image }}</td>
                <td>{{ ligne_panier.idPanier }}</td>
                <td>{{ ligne_panier.idProduit }}</td>
                <td>
                    <a href="{{ path('app_ligne_panier_show', {'idLigne': ligne_panier.idLigne}) }}">show</a>
                    <a href="{{ path('app_ligne_panier_edit', {'idLigne': ligne_panier.idLigne}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ligne_panier_new') }}">Create new</a>
{% endblock %}
